<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:PlannerTool.ViewModels"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="PlannerTool.Controls.TaskTab"
             x:DataType="vm:TaskViewModel">

        <Grid RowDefinitions="Auto, *" >
            <Border Grid.Row="0" CornerRadius="8" BorderThickness="1" BorderBrush="Gray" Background="Transparent" Padding="{Binding Padding}">
                <Grid ColumnDefinitions="*, Auto, Auto,Auto" ColumnSpacing="4">
                    <Grid Grid.Column="0"
                          ColumnDefinitions="Auto, *"
                          ColumnSpacing="0">
                        <Button Grid.Column="0"
                                Content="{Binding ShowSubTasksButtonContent}"
                                Classes="titleBarButton"
                                Command="{Binding ShowSubTasksCommand}"
                                IsVisible="{Binding HasSubTasks}" />
                        <TextBox Grid.Column="1"
                                 Text="{Binding Title}" />
                    </Grid>
                    <Button x:Name="ParentButton" Grid.Column="1" FontSize="16" FontFamily="{DynamicResource IconsBold}" Content="&#xe13c;">
                        <Button.Flyout>
                            <Flyout ShowMode="TransientWithDismissOnPointerMoveAway" Placement="Top">
                                    <StackPanel Width="{Binding Width, ElementName=ParentButton}">
                                        <Button FontSize="16"
                                                Content="&#xe4a6;"
                                                FontFamily="{DynamicResource Icons}"
                                                Command="{Binding DeleteTask}" 
                                                />
                                        <Button FontSize="16"
                                                Content="&#xE3D4;"
                                                FontFamily="{DynamicResource Icons}"
                                                Command="{Binding AddSubTask}" 
                                                />
                                    </StackPanel>
                            </Flyout>
                        </Button.Flyout>
                    </Button>
                    <Button Grid.Column="2"
                            Command="{Binding ChangeState}"
                            Background="Transparent"
                            BorderThickness="0"
                            Padding="0"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center">
                        <Ellipse Width="16"
                                 Height="16"
                                 Fill="{Binding StateColor}" />
                    </Button>
                </Grid>
            </Border>
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" IsVisible="{Binding ShowSubTasks}">
                <ItemsControl ItemsSource="{Binding SubTasks}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border CornerRadius="8" BorderThickness="1" BorderBrush="Gray" Background="Transparent" Padding="8" Margin="16,4,0,0">
                                <Grid ColumnDefinitions="Auto, *, Auto">
                                    <TextBox Grid.Column="0" FontSize="12" Text="{Binding Title}" />
                                    <Button Grid.Column="2" FontSize="16" FontFamily="{DynamicResource Icons}" 
                                            Content="&#xe4a6;"   
                                            Command="{Binding DeleteTask }"
                                            CommandParameter="{Binding}"></Button>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
</UserControl>
